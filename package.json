{
  "name": "sps-triggers",
  "license": "UNLICENSED",
  "scripts": {
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ sps-triggers",
    "create-local": "graph create --node http://localhost:8020/ sps-triggers",
    "remove-local": "graph remove --node http://localhost:8020/ sps-triggers",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 sps-triggers",
    "test": "graph test"
  },
  "dependencies": { "@graphprotocol/graph-cli": "0.68.5" }
}
